info:
  name: tickets/list
  type: http
  seq: 7

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/tickets?q=dashkoard button invoice"
  params:
    - name: q
      value: dashkoard button invoice
      type: query
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
