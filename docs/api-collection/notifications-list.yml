info:
  name: notifications/list
  type: http
  seq: 9

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/notifications"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
