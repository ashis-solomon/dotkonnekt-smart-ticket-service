info:
  name: reminders/create
  type: http
  seq: 10

http:
  method: POST
  url: "{{BASE_URL}}/api/v1/tickets/01a010c5-e1b8-7bcd-a90b-263414877d1e/reminders"
  body:
    type: json
    data: |-
      {
        "scheduled_for": "2026-08-17T18:18:00Z",
        "message": "Follow up with customer regarding invoice download fix"
      }
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
